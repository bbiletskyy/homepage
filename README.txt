My homepage written in Scala using Lift and jsMath (work in progress). 
In order to launch the application run mvn jetty:run. Open localhost:8080 in your browser to see the homepage.

In-memory database is used by default.

Currently my homepage is available at www.b-squared.org.ua


